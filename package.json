{
  "name": "chess-trainers",
  "version": "1.0.0",
  "description": "Coordinates + Puzzle Trainers using Chessground",
  "main": "index.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --outfile=dist/main.js && esbuild puzzle.ts --bundle --outfile=dist/puzzle.js && esbuild boardList.ts --bundle --format=esm --outfile=dist/boardList.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "chessground": "^9.1.1",
    "chess.js": "^1.0.0",
    "snabbdom": "^0.7.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.2",
    "typescript": "^5.8.3"
  }
}
