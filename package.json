{
  "name": "chess-trainers",
  "version": "1.0.0",
  "description": "Coordinates + Puzzle Trainers using Chessground",
  "main": "index.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --outfile=dist/main.js && esbuild puzzle.ts --bundle --outfile=dist/puzzle.js && esbuild view.ts --bundle --format=esm --outfile=dist/view.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "chess.js": "^1.4.0",
    "chessground": "^9.2.1",
    "snabbdom": "^0.7.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}
